type StenoChatProps = {
    id?: string;
    origin: string;
    url?: string;
    position?: 'left' | 'right';
};
declare const StenoChat: ({ id, origin, url, position }: StenoChatProps) => null;
export default StenoChat;
